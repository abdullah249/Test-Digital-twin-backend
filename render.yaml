services:
  - type: web
    name: digital-twin-backend
    env: node
    buildCommand: cd digitaltwin && npm install && npm run build
    startCommand: cd digitaltwin && npm start
    rootDir: digitaltwin

